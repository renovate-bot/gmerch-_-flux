---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: local-gemms-network-staging
  namespace: default
spec:
  secretName: local-gemms-network-staging-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  commonName: "*.local.gemms.network"
  dnsNames:
  - "local.gemms.network"
  - "*.local.gemms.network"
